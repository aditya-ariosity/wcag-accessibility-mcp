# Accessibility instruction for Claude Code

Use the a11y-feedback MCP tools for every generated or modified interface. Audit rendered output at desktop and mobile sizes, retain selector and DOM evidence, correct root components or tokens when possible, and rerun the same audit after changes. Treat the server as read-only. Keep human checks visible and never equate an automated pass with WCAG conformance.
